# .github/workflows/deploy-dev.yaml
name: Polybot Deploy Prod

on:
  workflow_call:
    inputs:
      image:
        required: true
        type: string
    secrets:
      EC2_HOST_DEV:
        required: true
      EC2_USERNAME:
        required: true
      EC2_SSH_KEY:
      DOCKERHUB_USERNAME:
        required: true
   




  workflow_dispatch:
    inputs:
      image:
        required: true
        type: string

jobs:
    Deploy:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v4
        - name: Deploy Docker image
          env:
            IMG_NAME: ${{ inputs.image }}
            EC2_HOST: ${{ secrets.EC2_HOST_DEV }}
            EC2_USERNAME: ${{ secrets.EC2_USERNAME }}
            SSH_PRIVATE_KEY: ${{ secrets.EC2_SSH_KEY }}
            DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
          
          
          run: |
          
            echo "IMG_NAME=${DOCKERHUB_USERNAME}/${IMG_NAME}" > .env
            mkdir -p ~/.ssh
            echo "$SSH_PRIVATE_KEY" > ~/.ssh/private_key
            chmod 600 ~/.ssh/private_key
            scp -i ~/.ssh/private_key -o StrictHostKeyChecking=no .env docker-compose.dev.yaml $EC2_USERNAME@$EC2_HOST:/home/$EC2_USERNAME/
            ssh -i ~/.ssh/private_key -o StrictHostKeyChecking=no $EC2_USERNAME@$EC2_HOST "docker compose -f docker-compose.dev.yaml pull && docker compose -f docker-compose.dev.yaml up -d"
